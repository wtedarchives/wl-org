/**
 * Canonical Setlist Archive URL for Echo of a Show (Setlist Game rebuild).
 * Root slug only until show/tour query params land on this route.
 */
export function getEchoArchiveIndexUrl(): string {
  return "/archive/echo"
}
