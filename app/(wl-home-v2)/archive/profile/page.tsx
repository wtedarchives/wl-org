import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "My Stats",
}

/** Tab UI and shell render in `WlHomeV2ArchiveProfileLayoutClient` (layout). */
export default function ArchiveProfilePage() {
  return null
}
