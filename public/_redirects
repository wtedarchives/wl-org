# Legacy path-based setlist URLs → query model (static export builds one HTML file)
/archive/setlist/*  /archive/setlist?id=:splat  301

# Legacy path-based year URLs → query model
/archive/years/*  /archive/years?id=:splat  301

# Legacy path-based tour URLs → query model
/archive/tours/*  /archive/tours?id=:splat  301
