# Legacy path-based setlist URLs → query model (static export builds one HTML file)
/archive/setlist/*  /archive/setlist?id=:splat  301

# Legacy path-based year URLs → query model
/archive/years/*  /archive/years?id=:splat  301

# Legacy path-based tour URLs → query model
/archive/tours/*  /archive/tours?id=:splat  301

# Legacy path-based song URLs → query model
/archive/song/*  /archive/song?id=:splat  301

# Legacy path-based personnel URLs → query model
/archive/personnel/*  /archive/personnel?id=:splat  301

# Legacy path-based discography URLs → query model
/archive/discography/*  /archive/discography?id=:splat  301

# Legacy path-based list URLs → query model
/archive/lists/*  /archive/lists?id=:splat  301
